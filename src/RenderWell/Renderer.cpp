
using namespace RenderWell;

    // Declare Constructor and Destructor
    // Declare copy and move constructors

    void Renderer::render(path){
        // Call to Poppler library to render the document at supplied path

        // Grab active page number and update member variable accordingly 

        // Set a daemon to retrieve active page number and update

        // Terminate daemon on close and cache page number to disk under EBook
    } 
